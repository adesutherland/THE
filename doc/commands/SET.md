# SET
**execute various set commands**

## Syntax
```text
SET set_command [set_command parameter(s) ...]
```

## Description
The SET command is a front end to existing SET commands. It treats the first parameter it receives as
a command and executes it.

## Compatibility
XEDIT: Compatible.
KEDIT: Compatible.

## Status
Complete.
